import { heroContent, navigationLinks, navbarAction } from '@/features/home/data/criticalContent';

function useHomePageContent() {
  return {
    heroContent,
    navigationLinks,
    navbarAction,
  };
}

export default useHomePageContent;
